# Shared Modules

Cross-service shared domain concepts (types) and helper logic.

Initial scaffold will be added later. Planned contents:

- Common domain types (e.g., Product, Variant, Listing, ProfitBreakdown)
- Shared validation helpers (input/output guards)
- Utility functions used across services (pricing/tax helpers, formatting)
- Test helpers for consistent fixtures across API/workers

